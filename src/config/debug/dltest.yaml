# @package _global_

defaults:
  - dflt.yaml

hydra:
  job_logging:
    loggers:
      uws4vad.data._data:
        level: DEBUG
      uws4vad.data.testdl:
        level: DEBUG

dataload:
  test:
    dryrun: True