defaults:
  - _dflt
  - cls: mlp

id: mir 
wght_init: xavier0 ## xavier uniform upon weihghts / bias 0

## the simplest example
## the classifier is build upon cls cfg if given
## if not pass parameters for the chosen in Network.init at _cfg
cls:
  hdim_ratio: 4
  do: 0.7