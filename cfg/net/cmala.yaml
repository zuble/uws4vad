defaults:
  - _.yaml
  - main: dflt
  - cls: conv
  - pstfwd: dflt

id: cmala
wght_init: xavier0 

main:
  _cfg:
    xat_hdim: 128 # match faud ? 
    ffn_hdim: 512 # half of frgb ?
    do: 0.1
    sigma: 0.06
    bias: 0.02
    
cls:
  ks: 7