defaults:
  - _globo.yaml
  
train:
  mil: 0
  #crops2use: ???
  #cropasvideo: False 
  
  ## uni -> picks len idxs equally spaced trough linspace 
  ## seq -> picks len idx sequential
  ## rnd -> picks len idx rnd keeping their temporal order (if mil on defaults to uni)
  sel: seq 
  len: 200
  ## avg adj sel snippet feats ;; if on defaults sel->uni 
  intplt: 0  
  rnd: 
    ## jit -> rnd choses adjcent snippets features
    ## glob -> rnd whole idxs of feat
    ## each crop will have the same rnd
    - null

test:
  crops2use: 1