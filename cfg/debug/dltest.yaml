# @package _global_

defaults:
  - dflt.yaml

hydra:
  job_logging:
    loggers:
      src.data._data:
        level: DEBUG
      src.data.testdl:
        level: DEBUG

dataload:
  test:
    dryrun: True