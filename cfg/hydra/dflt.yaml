defaults:
  - override job_logging: colorlog
  - override hydra_logging: colorlog

# output directory, generated dynamically on each run
run:
  dir: ${path.log_dir}/${name}/${task_name}/runs/${now:%d-%m}_${now:%H-%M-%S}
sweep:
  dir: ${path.log_dir}/${name}/${task_name}/multiruns/${now:%d-%m}_${now:%H-%M-%S}
  subdir: ${hydra.job.num}

#log_key: ${name}/${task_name}
job_logging: #disabled
  formatters:
    colorlog:
      format: '[%(black)s${name}%(reset)s:%(blue)s%(name)s%(reset)s:%(cyan)s%(lineno)s%(reset)s][%(log_color)s%(levelname)s%(reset)s] - %(message)s'
      ## [src.vldt.vldt:316][INFO] - $$$$ VALIDATE @ 00:00:06
